const path = require('path');

module.exports = {
  // Your existing configuration
  resolve: {
    alias: {
      // Add aliases if needed
    },
    extensions: ['.js', '.jsx', '.ts', '.tsx'], // Add file extensions if missing
  },
  // Other configurations
};
